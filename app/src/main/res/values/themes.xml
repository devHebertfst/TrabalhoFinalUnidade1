<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Hub -->
    <style name="Theme.Hub" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/hub_bg</item>
        <item name="android:textColor">@color/hub_text_primary</item>
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <!-- Basquete -->
    <style name="Theme.Basquete" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/basquete_team_a</item>
    </style>

    <!-- Calculadora -->
    <style name="Theme.Calculadora" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/hub_bg</item>
        <item name="android:textColor">@color/hub_text_primary</item>
        <item name="colorPrimary">@color/calc_btn_op_bg</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <!-- Quiz -->
    <style name="Theme.Quiz" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/quiz_bg</item>
        <item name="android:textColor">@color/quiz_text_body</item>
        <item name="colorPrimary">@color/quiz_btn_primary</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

</resources>